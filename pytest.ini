[pytest]
testpaths = tests

# Network tests are deselected by default so the suite is hermetic and fast.
# Run them explicitly with: pytest -m network
addopts = -m "not network"

markers =
    network: tests that download data from the internet (deselected by default)
    slow: long-running tests, such as tensor factorizations and permutations
